# 知幕 Zhimu Player 使用说明

本文适用于 `1.2.0-rc.8` 拟发布候选版。知幕是原 AI Video Player 的新名称，现有视频、字幕和导读继续使用；候选版尚不代表真实云端任务已完成验收，详见 [候选版本说明](./docs/RELEASE-1.2.0-rc.8.md)。

## 第一次打开

安装程序可选择简体中文或 English 以及安装目录，仅为当前 Windows 用户安装。便携版可直接运行，但登录态和播放状态仍保存在当前用户的应用数据目录，复制便携程序不会复制账号登录态。

首次引导顶部有直接可见的「中文｜English」按钮，点击后整套引导文案、步骤说明和提示立即切换并自动保存，不需要另外点保存。设置页顶部也提供相同按钮。随后引导依次完成：选择导读来源、登录或填写 API Key、按需登录听悟。可以先跳过云端功能，直接打开本地视频；以后在设置中补齐。

- ChatGPT 网页是默认来源。在普通 Edge 专用窗口手动完成登录，关闭该专用窗口后回播放器点“检查连接”；Plus 档使用 GPT-6 Astra / 极高，Pro 档使用 Astra Pro。登录阶段不进行自动浏览器操作；日常 Edge 不用关闭。账号实际没有该模型或档位时会提示失败，需要由用户重新选择。
- DeepSeek API 与 ChatGPT 共用一个安装包。在设置里切换来源，填写自己的 API Key，离开输入框后会自动获取模型，从列表选择后保存。设置中也可随时刷新。
- 需要自动生成字幕时，在听悟登录窗口完成登录，再返回检查，并按视频中实际语音选择中文或英语。已有字幕时可跳过。服务方登录页可能仍使用中文。

不必导出 Cookie，不必安装 Python、ASR Skill 或 Codex。ChatGPT 网页路线需 Microsoft Edge。账号密码和验证码请只在服务方登录页面输入。

## 打开视频

- 点击“打开文件夹”，该文件夹直接包含的视频会组成一套播放列表，不扫描子文件夹。
- 点击“打开单个视频”，播放器会建立只有一项的播放列表，不自动加入同目录其他视频。
- 也可以把一个视频文件或文件夹直接拖到播放窗口。
- 文件夹内视频按自然文件名排列，例如 P1、P2、P10。之后可以拖动调整顺序，或点击“按文件名重排”。

播放器会恢复上次的播放列表、当前 Part、播放位置、倍速和观看状态。

## 右侧工作区

鼠标移到窗口最右侧会展开工作区，移开后自动收回。右上角图钉可以将其固定，固定后可拖动左侧边缘调整宽度。

工作区只有三个标签：

- 播放列表：切换 Part、拖动排序、查看进度、标记已看完。
- 字幕：浏览完整字幕并点击跳转。
- 导读：读取和切换本地 Markdown 导读版本，点击时间戳跨 Part 跳转。

固定侧栏进入全屏后仍然保留。临时侧栏覆盖画面，固定侧栏才参与布局；视频始终保持原始宽高比。

## 播放快捷键

| 操作 | 快捷键 |
|---|---|
| 播放／暂停 | 空格 |
| 后退／前进 1 秒 | 左／右方向键 |
| 后退／前进 10 秒 | Ctrl + 左／右方向键 |
| 后退／前进 30 秒 | Shift + 左／右方向键 |
| 调节音量 | 上／下方向键 |
| 静音 | M |
| 显示／隐藏字幕 | S |
| 整窗全屏 | F |
| 退出全屏 | Esc |
| 搜索字幕与导读 | Ctrl + F |

单击视频画面播放或暂停，双击切换整窗全屏。播放时鼠标静止约两秒后控制栏自动隐藏。

## 字幕

播放列表存在缺失字幕时，列表上方会出现字幕任务条。点击“生成字幕”后，播放器通过内置听悟流程串行上传所选媒体，并在视频同目录生成同名 SRT。本版内置转写流程不另外生成转写 Markdown。任务完成后播放列表会自动重新加载字幕；取消只停止本地任务，不会删除已经提交到听悟云端的记录。字幕已经齐全时，任务条自动隐藏。

听悟登录态失效时，在设置里重新打开听悟登录窗口，由用户完成登录，再返回检查。播放器不会自动转为另一项付费转写服务。

如果视频目录已有同名 SRT，但内容为空或格式无效，播放器不会把它当作成功，也不会直接覆盖。请先将该文件更名备份，再重试生成字幕。

播放器按以下顺序自动匹配视频字幕：

1. 视频名.YYYYMMDD-HHMMSS.srt，存在多份时选择时间戳最新的一份。
2. 没有修订版时使用精确同名的 视频名.srt。

字幕浏览器会约每秒跟随当前字幕一次。手动滚动后暂停跟随，点击“回到当前字幕”恢复。

单击一条字幕会跳转播放位置。需要修正时右键选择“编辑这条字幕”，可修改文字、开始时间和结束时间。保存后不会覆盖原字幕，而是在视频目录生成新的带时间戳 SRT；同一次运行中的后续修改继续写入该修订文件。

字幕设置包括字号、显示开关、时间偏移、垂直位置和背景透明度。时间偏移按视频保存。

## 内容导读

导读面板的语言选项控制**下一次生成**的输出：简体中文、English，或跟随字幕的主要语言。它不修改已经保存的导读、字幕和文件名。界面语言、导读输出语言和听悟语音语言独立设置；例如，可以用英文界面、按中文识别视频，再生成英文导读。

新用户导读默认“跟随字幕”；旧配置没有该字段时继续生成中文导读。听悟语音的新用户初始选择参考系统语言，但应按实际视频手动确认；旧配置没有该字段时继续使用中文。

- 已有导读 Markdown 会直接读取，不自动重新生成。
- 同一文件夹可以保留多份导读，右侧版本下拉框切换当前版本。
- 新导读会记录生成时每个 Part 对应的视频文件名。点击 [P2-01:23:45] 等时间戳时，按该记录找到视频再跳转；调整播放列表顺序不会把链接移到另一个视频。
- 点击“编辑”可修改当前版本，修改会在原文件中自动保存。
- 生成新导读前会检查整套播放列表的字幕；只要有一个 Part 缺字幕就阻止任务。

旧的多视频导读若未保存可靠的原始 Part 对应记录，时间戳跳转会停用，正文仍可阅读、编辑和搜索；重新生成导读后可恢复跳转。只有一项视频的旧导读仍可跳转到该视频。视频改名、移除或出现同名文件时，无法唯一对应的时间戳不会跳转，其余链接继续可用。

对应记录保存在导读 Markdown 开头的隐藏说明中，播放器显示和编辑正文时会保留它。外部编辑时请勿删除该记录；记录损坏后跳转会停用。

“导读生成方式”默认选择 ChatGPT 网页。在播放器专用的 Edge 窗口中登录；它与日常 Edge 配置分开。新用户可直接使用普通新会话，无需预先创建某个固定名称的 ChatGPT 项目。

点击“用 ChatGPT 生成”后，播放器按设置选择模型和推理档位，发送字幕任务，等待回答并保存成视频目录中的新导读版本。明确选择项目时，会核对对话归属；归属不符不会保存。网页适配失败会提示当前阶段，不会静默改用 DeepSeek。需要 agent 排查时提供错误文字即可，不要发送完整 Cookie 或用户数据目录。

为避开网页附件上传的不稳定性，12 万字符以内的任务会直接写入对话；更长任务才使用文本附件，并等待文件原始内容上传完成后再发送。两种方式的输出都保存为 Markdown。

DeepSeek API 在同一个设置页中选择，只有明确选中并保存后，生成按钮才会调用该接口。填写 Key 或 API 地址后离开输入框，或打开已保存 Key 的 API 设置时，会通过接口的 `/models` 获取列表；也可点击“刷新模型”。这个操作不发送字幕、不生成导读。API Key 与网页订阅独立，调用费用由 API 服务方收取。

在 ChatGPT 和 DeepSeek 之间切换会保留两边已有设置、ChatGPT 登录资料和已保存的 DeepSeek Key。切回后无需因为切换本身重新登录或填写 Key。设置页的“登录 ChatGPT”是操作入口，不表示账号已退出；可点击“检查连接”确认服务端是否仍认可登录。主动退出账号、明确删除 Key 或服务方使会话过期另行处理。

列表中的名称由接口返回，程序不再写死允许使用的模型。原有配置保留原来的模型名；当前模型不在最新列表中时，会提示重新选择或手动确认，不会自动更换。临时网络故障可显示本次启动中缓存的同一接口、同一 Key 的列表；缓存会明确标注，重启后不保留，认证失败后清除。切换接口不会把已保存的 Key 发往其他网站。兼容接口不支持模型列表时，在高级设置手动填写准确名称。列表只提供名称，不表示各模型的能力或价格。

## 本地状态与文件

- 更名后继续使用原有应用数据目录，原配置、播放进度和登录态无需因更名手动搬移。
- 播放器状态保存在 Electron 用户数据目录的 player-state.json；为兼容旧版，用户数据目录名仍为 `video-player`，看到旧目录名是正常的。
- 视频和原始字幕不会被修改。
- 字幕修订版及导读版本保存在对应视频目录。
- 播放器不会自动删除听悟或其他云端服务中的记录。

## 视频兼容性

界面可以选择 MP4、MKV、AVI、WebM、MOV、M4V 和 TS 文件，但是否能直接播放仍取决于文件内部的音视频编码。遇到 Chromium 无法解码的编码时，播放器会明确提示，不会修改或自动转码原视频。
