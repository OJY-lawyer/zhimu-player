# 知幕 Zhimu Player Design Contract

## Product intent

知幕 Zhimu Player 是一个服务长直播、课程和多 Part 录像回看的本地 AI 视频阅读工作台。画面始终是主角，播放列表、字幕与内容导读只在用户需要定位时出现。

产品显示名统一为「知幕」和 `Zhimu Player`；`1.2.0-rc.1` 是更名后的拟发布候选版。品牌更名沿用现有暖黑与陶土色设计，不增加新的业务功能；原有存储和导读协议标识保持兼容。

## Experience principles

1. **Video first**：默认界面只保留必要窗口标题与悬浮播放控件，不让列表或设置长期侵占画面。
2. **Reveal on intent**：右侧边缘唤出工作抽屉，钉住后才参与布局；临时查看不重排画面。
3. **Preserve the source**：视频始终等比例 `contain`，任何窗口尺寸、全屏或抽屉宽度都不得拉伸画面。
4. **Text is navigation**：字幕、导读、搜索结果中的时间戳都是可点击导航，不做装饰性信息堆叠。
5. **Calm, legible, local**：暖黑背景、陶土强调色、克制动效；状态明确，不使用云端产品式大卡片和营销文案。

## Visual direction

- 背景：炭黑与暖灰黑，视频舞台使用接近纯黑。
- 强调：陶土橙；只用于当前项、进度、焦点和主要动作。
- 文本：暖白主文字、灰米色次文字，避免纯白大面积刺眼。
- 边界：一像素暖灰描边和柔和阴影；圆角小而克制。
- 字体：Windows 原生中英文 UI 字体栈；数字时间使用等宽数字特性，英文文案应允许自然换行。
- 字级：元信息 13px、正文与常用操作 15px、面板标题 17px；不再用一组全局选择器把不同层级统一放大。
- 图标：统一线性 SVG；禁止 emoji、字母按钮和含义不清的“览／总”。
- 应用标识：炭黑圆角底板上的陶土色章节路径与暖白定位点；表达长视频时间轴、章节穿行和精确跳转，避免传统播放三角、胶片、摄像机和 AI 星光符号。

## Anti-direction

- 不使用紫色渐变、玻璃拟态、发光霓虹、巨型圆角卡片或大面积留白。
- 不保留“单视频／播放列表”模式切换，不同时显示左右两套侧栏。
- 不用动画掩盖状态变化；抽屉和控件过渡控制在约 120–220ms。

## Layout

- 标题栏：46px，显示播放列表名和当前 Part，提供置顶、最小化、最大化、关闭。
- 播放舞台：占据剩余空间，视频、悬浮字幕、控制栏、进度预览均在同一层级体系内。
- 右侧抽屉：默认 420px，最小 300px，最大为窗口宽度 45%；标签为播放列表、字幕、导读。
- 临时抽屉覆盖舞台；钉住抽屉成为右侧布局列。右侧 10px 感应区在窗口和全屏下均可用。
- 搜索：`Ctrl+F` 临时覆盖，不常驻占位。

## Provider hierarchy

- 一个安装包，默认 ChatGPT 网页；Plus 方案为 GPT-6 Astra 极高，Pro 方案为 Astra Pro，按用户选择并在生成前核验可见模型与档位。
- DeepSeek API 在同一个设置页选择，通过 `/models` 自动获取模型，支持刷新、标注缓存和高级手动填写；保留已有选择，不自动替换。API Key 由主进程加密保存和使用。
- 不自动切换来源或降低档位；一次生成失败由用户决定重试，避免重复消费。
- 新用户无需指定 ChatGPT 项目；已有项目可在高级设置中填写。
- 首次使用只展示三步向导，可以跳过并本地播放。设置复用相同控件，高级项目名/接口默认收起。
- 界面语言选择放在首次引导与设置中，不新增常驻工具栏。导读面板就地选择输出语言（简体中文 / English / 跟随字幕）；听悟设置就地选择视频语音（中文 / 英语）。三项互不联动，不翻译用户内容或已有文件。
- 应用控件和已知状态错误使用明确的中英文词条；英文界面遇到未知中文服务详情时保留原文并加英文说明，不能隐藏诊断信息或假装已经翻译。服务方页面保持原样。
- 关于页保留作者欧俊言律师，微信与赞助可按需展开；二维码原像素保留，新框架采用暖黑陶土色。

## Task surfaces

- 长任务在所属面板内显示单行状态，不弹出占据视频的阻塞窗口。
- 导读任务依次显示准备、进入项目、选择模型、上传、等待回答、保存；运行中提供取消。
- ASR 只在缺字幕、运行或失败时出现任务条；字幕齐全时不常驻一个禁用按钮。

## Interaction contract

- 播放时鼠标静止约 2 秒隐藏控制栏；暂停、拖动、悬停或弹层打开时保持可见。
- 单击画面播放／暂停，双击或 `F` 切换整窗全屏，`Esc` 退出。
- 方向键 ±1 秒，`Ctrl` + 方向键 ±10 秒，`Shift` + 方向键 ±30 秒；上下调音量，`M` 静音，`S` 切换字幕。
- 字幕浏览器约每秒居中跟随一次；用户手动滚动后暂停，点击“回到当前字幕”恢复。
- 单击字幕跳转；右键才出现低频编辑入口。
- 状态不只依赖颜色：当前项、缺字幕、未校对、新增、已看完都必须同时有文字或图形标识。
