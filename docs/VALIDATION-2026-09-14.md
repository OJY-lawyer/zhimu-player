# 2026-09-14 验证记录

> 这是 **1.0.0 的历史记录**，保留当时模型设置和验证范围。1.1.0 的英文适配与 Astra Pro
> 变更另见 [1.1.0 验证记录](./VALIDATION-1.1.0.md)，不沿用本页结果宣称新版已验收。

本记录区分离线检查、真实桌面操作和真实云端业务。根据本轮主代理已执行的验证记录
整理；未实际完成的项目明确保留。它不是全流程可用承诺，也不替代后续发布复验。

## 已执行

| 范围 | 已取得的证据 | 结论边界 |
|---|---|---|
| 类型与离线回归 | 最终源码的类型检查和 8 组离线测试再次通过，覆盖文件拖入解析、导读规则与状态、Part 对应记录、来源安全、听悟客户端/会话/任务逻辑 | 使用测试数据，不涉及真实账号上传 |
| Electron 桌面 UI | 用本次 Electron 44 运行 ASAR 内的实际主程序和隔离用户数据，复验三步引导、设置内两种来源、关于页、900×600 窗口；重载不重复出现引导，关闭握手通过，控制台错误为空 | 验证了打包后程序，不等于 NSIS 实际安装或直接运行便携封装 EXE |
| 本机加密存储 | 上述实际程序内的原生 `safeStorage` 加密、解密往返通过 | 证明本机该次往返，未覆盖另一 Windows 用户或另一台机器 |
| Windows 构建 | `npm run build` 成功产生 NSIS 安装程序和便携版，最终构建无平台兼容警告；EXE 产品名为 AI Video Player，文件版本 1.0.0.0 | 两个文件均未签名（NotSigned），尚未实际执行安装/卸载或从便携封装启动 |
| 包内容 | ASAR 包含 123 个文件，9 个 dist 文件与最终构建逐字节一致，禁入文件为 0；外部 resources 保留 LICENSE、NOTICE 和第三方许可原文 | 验证包结构和内容一致性，不证明云端服务可用 |
| 依赖审计 | 升级后完整 `npm audit` 报告 0 个已知漏洞 | 只反映当次依赖锁文件与 npm 漏洞库 |
| 公开扫描 | 白名单文件扫描未发现配置、私有路径或凭据规则命中 | 文本规则扫描不能识别一切隐私；三个关于页图片经作者明确授权公开 |
| 赞助码一致性 | 新海报和原收款码均通过 OpenCV 解码，解码内容一致 | 未进行付款；不回显二维码负载或收款标识 |

## 尚未完成或未通过

| 范围 | 当前状态 | 仍需验证 |
|---|---|---|
| ChatGPT 登录 | 现有播放器专用浏览器配置探测返回 `authenticated: false`，登录态已过期 | 用户在专用窗口重新登录并再次探测 |
| ChatGPT Plus / Pro | 本轮未完成真实模型生成 | 分别核对 Plus 的 Astra / 极高、Pro 的 Sol / Pro，以及发送、等待、保存和加载 |
| 听悟 | 本轮未完成真实账号登录、上传或转写 | 用户登录后上传一段授权短媒体，回收 SRT 并自动加载；当前内置流程只生成 SRT |
| DeepSeek API | 本轮未完成真实账号鉴权或生成 | 使用用户自己的 Key 与余额，验证所选模型、返回、保存及错误处理 |
| 安装与便携封装 | 构建及 ASAR 内实际程序验证已完成，未执行 NSIS 实际安装/卸载，也未直接运行便携封装 EXE | 在干净的 Windows 用户环境验证可选目录、当前用户安装、卸载和便携启动 |
| 跨机器和长视频 | 未覆盖全新机器、长播放列表或所有视频编码 | 独立记录环境、媒体范围与实际结果 |

ChatGPT 登录过期不是新模型支持已验收的证据。界面成功、离线测试成功、二维码可
解码，也分别不能替代真实模型生成、云端转写或用户实际付款。截止本记录更新时，
没有创建或发布 GitHub 仓库，也没有发起付款。

## 本轮行为补充

新导读保存 Part 与视频文件名的对应记录；重排后仍按原记录定位，旧多视频无可靠
记录时保留阅读、编辑和搜索，停用推测性的跳转。听悟输出只有 SRT；空白或格式无效
的同名 SRT 不再被当作完成依据。已有文件被保留，界面提示先更名备份后再重试。

旧的导读与配置状态入口及本机依赖安装批处理已退役，保留在本机工作备份中，不进入
公开源码。公开包以当前实现及锁文件为准，不需要恢复开发者的私人脚本、配置或 Skill。
